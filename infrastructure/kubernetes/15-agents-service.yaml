apiVersion: apps/v1
kind: Deployment
metadata:
  name: smm-agents-service
  namespace: smm-architect
  labels:
    app.kubernetes.io/name: smm-architect
    app.kubernetes.io/component: agents-service
    app.kubernetes.io/version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: smm-agents-service
  template:
    metadata:
      labels:
        app: smm-agents-service
        app.kubernetes.io/name: smm-architect
        app.kubernetes.io/component: agents-service
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      serviceAccountName: smm-architect-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
      containers:
      - name: agents
        image: smm-architect/agents:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3004
          name: http
          protocol: TCP
        env:
        - name: PORT
          value: "3004"
        - name: NODE_ENV
          value: "production"
        envFrom:
        - configMapRef:
            name: smm-architect-config
        - secretRef:
            name: smm-architect-secrets
        resources:
          requests:
            memory: "2Gi"
            cpu: "1000m"
          limits:
            memory: "4Gi"
            cpu: "2000m"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 45
          periodSeconds: 15
          timeoutSeconds: 10
          successThreshold: 1
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
          successThreshold: 1
          failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        - name: logs
          mountPath: /app/logs
      volumes:
      - name: tmp
        emptyDir: {}
      - name: logs
        emptyDir: {}
      restartPolicy: Always
      terminationGracePeriodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: smm-agents-service
  namespace: smm-architect
  labels:
    app.kubernetes.io/name: smm-architect
    app.kubernetes.io/component: agents-service
spec:
  type: ClusterIP
  ports:
  - port: 3004
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: smm-agents-service